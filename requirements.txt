numpy==2.5.2
scipy==1.18.0
pandas==3.0.5
matplotlib==3.11.1
scikit-learn==1.9.0
mne==1.12.1
lightgbm==4.7.0
# PyTorch installed separately for CUDA 12.4:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cu124
# (tested with torch==2.6.0+cu124; CPU-only torch works but is slower)
